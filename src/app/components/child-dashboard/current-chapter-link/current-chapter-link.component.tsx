import React from 'react';

const CurrentChapterLink: React.FC = (props) => {
    React.useEffect(() => {
        // look up week # for child
    }, []);

    return <a href='/'>Current Story</a>;
};

export { CurrentChapterLink };
